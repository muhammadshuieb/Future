# High load / many users — include from radiusd.conf
max_requests = 4096

thread pool {
    start_servers = 10
    max_servers = 64
    min_spare_servers = 5
    max_spare_servers = 20
    max_requests_per_server = 0
}
