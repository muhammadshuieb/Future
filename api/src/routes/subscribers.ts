/**
 * Subscriber + FreeRADIUS integration routes (REST).
 * Implementation lives in subscribers.routes.ts; this module matches the project layout spec.
 */
export { default } from "./subscribers.routes.js";
