services:
  mysql:
    build:
      context: .
      dockerfile: docker/mysql/Dockerfile
    image: futureradius-mysql:local
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:?set MYSQL_ROOT_PASSWORD in .env}
      MYSQL_DATABASE: ${MYSQL_DATABASE:-radius}
      MYSQL_INITDB_SKIP_TZINFO: "1"
    ports:
      - "127.0.0.1:3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h localhost -p$${MYSQL_ROOT_PASSWORD}"]
      interval: 5s
      timeout: 5s
      retries: 10

  # RADIUS auth/accounting must be published: 1812/1813 UDP. If Portainer shows only 3799,
  # rebuild from this compose file — otherwise MikroTik will only "timeout".
  freeradius:
    build:
      context: .
      dockerfile: docker/freeradius/Dockerfile
    image: futureradius-freeradius:local
    ports:
      - "1812:1812/udp"
      - "1813:1813/udp"
      - "3799:3799/udp"
    environment:
      MYSQL_HOST: mysql
      MYSQL_USER: ${RADIUS_DB_USER:-radius}
      MYSQL_PASSWORD: ${RADIUS_DB_PASSWORD:?set RADIUS_DB_PASSWORD in .env}
      FREERADIUS_DEBUG: ${FREERADIUS_DEBUG:-0}
    depends_on:
      mysql:
        condition: service_healthy
      api:
        condition: service_healthy
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    ports:
      - "127.0.0.1:6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  # WAHA listens on 3000 inside the container; host 3001 avoids clashing with the API (3000).
  waha:
    image: devlikeapro/waha:latest
    ports:
      - "3001:3000"
    environment:
      WAHA_PRINT_QR: "true"
      WAHA_LOG_LEVEL: "info"
      WAHA_API_KEY: ${WAHA_API_KEY:?set WAHA_API_KEY in .env}
      WAHA_DASHBOARD_USERNAME: ${WAHA_DASHBOARD_USERNAME:?set WAHA_DASHBOARD_USERNAME in .env}
      WAHA_DASHBOARD_PASSWORD: ${WAHA_DASHBOARD_PASSWORD:?set WAHA_DASHBOARD_PASSWORD in .env}
      WHATSAPP_SWAGGER_USERNAME: ${WHATSAPP_SWAGGER_USERNAME:?set WHATSAPP_SWAGGER_USERNAME in .env}
      WHATSAPP_SWAGGER_PASSWORD: ${WHATSAPP_SWAGGER_PASSWORD:?set WHATSAPP_SWAGGER_PASSWORD in .env}
    volumes:
      - waha_sessions:/app/.sessions
    restart: unless-stopped

  api:
    build:
      context: ./api
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      DATABASE_URL: mysql://root:${MYSQL_ROOT_PASSWORD}@mysql:3306/${MYSQL_DATABASE:-radius}
      REDIS_URL: redis://redis:6379
      JWT_SECRET: ${JWT_SECRET:?set JWT_SECRET in .env}
      AES_SECRET_KEY: ${AES_SECRET_KEY:?set AES_SECRET_KEY in .env}
      DEFAULT_TENANT_ID: 00000000-0000-0000-0000-000000000001
      PORT: "3000"
      BACKUP_DIR: /app/backups
      BACKUP_RETENTION_DAYS: "7"
      WAHA_API_KEY: ${WAHA_API_KEY:?set WAHA_API_KEY in .env}
      WAHA_INTERNAL_URL: ${WAHA_INTERNAL_URL:-http://waha:3000}
      WAHA_SESSION_NAME: ${WAHA_SESSION_NAME:-default}
      SEED_ADMIN_EMAIL: ${SEED_ADMIN_EMAIL:-admin@local.test}
      SEED_ADMIN_NAME: ${SEED_ADMIN_NAME:-admin}
      SEED_ADMIN_PASSWORD: ${SEED_ADMIN_PASSWORD:-muhammadshuieb}
      RADIUS_DB_USER: ${RADIUS_DB_USER:-radius}
      RADIUS_DB_PASSWORD: ${RADIUS_DB_PASSWORD:?set RADIUS_DB_PASSWORD in .env}
      SQL_MIGRATIONS_DIR: /app/sql/migrations
    volumes:
      - backup_data:/app/backups
      - ./sql:/app/sql:ro
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      # Use Node (always present); curl may be missing in older api images → permanent unhealthy.
      test:
        [
          "CMD",
          "node",
          "-e",
          "fetch('http://127.0.0.1:3000/health').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))",
        ]
      interval: 15s
      timeout: 5s
      retries: 8
      start_period: 90s

  worker:
    build:
      context: ./api
      dockerfile: Dockerfile
    command: ["node", "dist/workers/index.js"]
    environment:
      DATABASE_URL: mysql://root:${MYSQL_ROOT_PASSWORD}@mysql:3306/${MYSQL_DATABASE:-radius}
      REDIS_URL: redis://redis:6379
      JWT_SECRET: ${JWT_SECRET:?set JWT_SECRET in .env}
      AES_SECRET_KEY: ${AES_SECRET_KEY:?set AES_SECRET_KEY in .env}
      DEFAULT_TENANT_ID: 00000000-0000-0000-0000-000000000001
      BACKUP_DIR: /app/backups
      BACKUP_RETENTION_DAYS: "7"
      WAHA_API_KEY: ${WAHA_API_KEY:?set WAHA_API_KEY in .env}
      WAHA_INTERNAL_URL: ${WAHA_INTERNAL_URL:-http://waha:3000}
      WAHA_SESSION_NAME: ${WAHA_SESSION_NAME:-default}
    volumes:
      - backup_data:/app/backups
      - ./sql:/app/sql:ro
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy

  # Admin UI: nginx serves on 80 in-container; browse http://HOST:8080
  web:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "8080:80"
    depends_on:
      api:
        condition: service_healthy

  # Optional PPTP VPN server for linking remote NAS nodes.
  # Run with: docker compose --profile vpn up -d --build pptp-vpn
  # NOTE: PPTP needs TCP/1723 + GRE(protocol 47) allowed on host firewall.
  pptp-vpn:
    profiles: ["vpn"]
    build:
      context: .
      dockerfile: docker/pptp/Dockerfile
    image: futureradius-pptp:local
    network_mode: host
    privileged: true
    environment:
      PPTP_SERVER_LOCAL_IP: ${PPTP_SERVER_LOCAL_IP:-10.10.10.1}
      PPTP_CLIENT_IP_RANGE: ${PPTP_CLIENT_IP_RANGE:-10.10.10.100-10.10.10.240}
      PPTP_DNS1: ${PPTP_DNS1:-1.1.1.1}
      PPTP_DNS2: ${PPTP_DNS2:-8.8.8.8}
      PPTP_MTU: ${PPTP_MTU:-1400}
      PPTP_MRU: ${PPTP_MRU:-1400}
      PPTP_USERS: ${PPTP_USERS:-}
    restart: unless-stopped

volumes:
  mysql_data:
  backup_data:
  waha_sessions:
